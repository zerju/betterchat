import { MissingImagePipe } from './missing-image.pipe';

describe('MissingImagePipe', () => {
  it('create an instance', () => {
    const pipe = new MissingImagePipe();
    expect(pipe).toBeTruthy();
  });
});
