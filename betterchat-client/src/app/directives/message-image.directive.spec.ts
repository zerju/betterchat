import { MessageImageDirective } from './message-image.directive';

describe('MessageImageDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageImageDirective();
    expect(directive).toBeTruthy();
  });
});
